const config = {
  apiUrl: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api',
  imagePath: process.env.NEXT_PUBLIC_IMAGE_PATH || 'http://localhost:8000/uploads',
};

export default config; 